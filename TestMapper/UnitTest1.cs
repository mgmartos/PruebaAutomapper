using Microsoft.VisualStudio.TestTools.UnitTesting;

using Prueba_Automapper;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace TestMapper
{
    [TestClass]
    public class UnitTest1
    {
        private Prueba_Automapper.Controllers.RepoFake _repo;

        [TestMethod]
        public async Task TestMethod1() {

           //_repo = new Prueba_Automapper.Controllers.RepoFake();
            //Task<List<string>>
           // var resultado =  await _repo.DameEditoriales();
            //Assert.IsNotNull(resultado);
            //Assert.IsTrue(resultado.Count > 10000);
          //  Assert.IsInstanceOfType(resultado, typeof(System.Collections.Generic.List<>));
        }
        //[TestMethod]
        //public void TestMethod2()
        //{

        //    _repo = new Prueba_Automapper.Controllers.RepoFake();
        //    var resultado =  _repo.DameEditoriales();
        //    //Assert.IsNotNull(resultado);
        //    Assert.IsTrue(resultado. > 0);
        //    //  Assert.IsInstanceOfType(resultado, typeof(System.Collections.Generic.List<>));
        //}

    }
}
